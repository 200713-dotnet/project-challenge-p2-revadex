@model StarDex.Client.Models.ConstellationViewModel

@{
    ViewData["Title"] = "Constellation";
}

<section>
  <h1>@Model.name</h1>
  <img src="@Model.imageURL" alt="@Model.name Image">
  <p>@Model.description</p>
  
</section>